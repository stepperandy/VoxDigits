import AIContentPage from '@/components/landing/AIContentPage';
export default function KillSwitch() {
  return <AIContentPage topic="VPN Kill Switch - automatic kill switch cuts internet if VPN drops, no IP leaks, always protected" badge="Kill Switch" emoji="⚡" ctaLabel="Get Protected Now" />;
}