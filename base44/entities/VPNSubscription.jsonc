{
  "name": "VPNSubscription",
  "type": "object",
  "properties": {
    "user_email": {
      "type": "string",
      "description": "Email of the VPN subscriber"
    },
    "plan": {
      "type": "string",
      "enum": [
        "Free Trial",
        "Pro Monthly",
        "Pro Annual",
        "Business",
        "Basic",
        "Standard",
        "Premium",
        "Advanced",
        "Enterprise"
      ],
      "description": "Subscription plan type"
    },
    "status": {
      "type": "string",
      "enum": [
        "active",
        "trial",
        "pending_payment",
        "expired",
        "cancelled",
        "paused"
      ],
      "default": "pending_payment",
      "description": "Current subscription status. pending_payment = signed up but not yet paid."
    },
    "billing_cycle": {
      "type": "string",
      "enum": [
        "monthly",
        "yearly",
        "trial",
        "custom"
      ],
      "description": "Billing cycle"
    },
    "price": {
      "type": "number",
      "description": "Current subscription price"
    },
    "start_date": {
      "type": "string",
      "format": "date-time",
      "description": "Subscription start date"
    },
    "renewal_date": {
      "type": "string",
      "format": "date-time",
      "description": "Next renewal / expiry date"
    },
    "max_devices": {
      "type": "integer",
      "description": "Maximum number of linked devices"
    },
    "stripe_subscription_id": {
      "type": "string",
      "description": "Stripe subscription ID for billing"
    },
    "stripe_customer_id": {
      "type": "string",
      "description": "Stripe customer ID created during $0 payment authorization"
    },
    "payment_method_id": {
      "type": "string",
      "description": "Stripe payment method ID saved during $0 authorization, charged after credits run out"
    },
    "payment_authorized": {
      "type": "boolean",
      "default": false,
      "description": "Whether the user completed the $0 card authorization required before free credits"
    },
    "credits_remaining": {
      "type": "integer",
      "default": 0,
      "description": "Free credits remaining (1 credit = 1 VPN connection). 0 means the trial is exhausted."
    },
    "credits_total": {
      "type": "integer",
      "default": 0,
      "description": "Total free credits granted for this trial (25)"
    },
    "credits_used": {
      "type": "integer",
      "default": 0,
      "description": "Free credits consumed so far"
    },
    "download_link": {
      "type": "string",
      "description": "Override download URL (admin-configurable)"
    },
    "notes": {
      "type": "string",
      "description": "Admin internal notes and device lock tags (device:DEVICE_ID)"
    }
  },
  "required": [
    "user_email",
    "plan",
    "status"
  ]
}